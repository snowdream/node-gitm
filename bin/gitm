#!/usr/bin/env node

var pjson = require('../package.json');
var program = require('commander');

program
    .version(pjson.version)
    .command('clone', 'clone all git repositories.')
    .parse(process.argv);